[package]
name = "zstd-sys"
version = "2.0.15"
links = "zstd"
edition = "2021"

[lib]
path = "src/lib.rs"

[build-dependencies]
cc = "1.2.30"
bindgen = "0.72.0"

[dependencies]
libc = "0.2.174"

[features]
default = ["legacy"]
legacy = []
